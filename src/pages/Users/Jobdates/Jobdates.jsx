


export const Jobdates = () => {
    //const user = sessionStorage.getItem('passport.token');
    //console.log(user);

    //const userInfo = bringProfileCall(user.token);
    //console.log(userInfo);

    return (
        <>
            <h1>Aqui Jobdate Representando</h1>
            
        </>
    )
}